# Project Rules

- 默认用中文沟通；代码、命令、日志和报错保持原语言。
- 处理问题时先定位根因和所属架构边界，不只做局部兜底或单点补丁；如果发现同类问题会继续出现，应优先抽象成清晰的策略层、服务边界或可测试规则，避免堆积屎山代码。
- 新增功能、修复缺陷或调整行为前，先检查现有调用链、数据流和测试覆盖；改动应服务于长期结构清晰，而不是把特殊情况散落到多个模块。
- 如果架构、核心流程、评分规则、训练闭环或重要设计发生变化，必须同步更新项目文档，说明新的职责边界、数据流和后续扩展方式。
- 每完成一个功能、修复或配置改动，并完成相应测试或验证后，必须立即执行一次 `git commit`；用户口头说的 `cm` 即指这一步。
- 提交前先检查 `git status`，确保 commit 只包含本次任务相关改动。
- 如果测试或验证无法运行、失败，或工作区存在不属于本次任务的改动，不要默默提交；先说明原因、风险和建议处理方式。
